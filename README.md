# HabitLadder — трекер полезных привычек

## Описание:
Backend-часть SPA приложения для формирования полезных привычек, основанная на идеях книги **«Атомные привычки» Джеймса Клира**.

Проект включает расширенный функционал: Telegram-уведомления, Celery-задачи, активацию аккаунта по email и строгую систему прав доступа.

## 🚀Основной функционал

### Проект реализует:
- CRUD для привычек
- Регистрацию и авторизацию пользователей
- Активацию профиля по email через Celery (отложенная задача)
- Интеграцию с Telegram для рассылки напоминаний
- Автоматические Celery-задачи:
  - отправка уведомлений о привычках
  - отправка писем активации профиля
- Систему прав доступа
- Пагинацию
- CORS для работы с фронтендом
- Покрытие тестами на 90%
- Документацию API

### Технологии
Python 3.12

Django 5.2.8

Django REST Framework

Celery + Redis

Requests

PostgreSQL

ZoneInfo (таймзоны)

Coverage (90%)

Flake8 (100%, исключая миграции)

## 🔧 Установка и запуск
1. Клонируйте репозиторий
````
git clone https://github.com/nadezhdapopova-spec/HabitLadder.git
cd HabitLadder
````
2. Установите зависимости
````
pip install -r requirements.txt
или
poetry install
````
3. Заполните .env
````
DJANGO_SECRET_KEY=your_secret_key_here
DEBUG=False

DB_NAME=your_database_name_here
DB_USER=your_database_user_here
DB_HOST=db
DB_PORT=5432
DB_PASSWORD=your_database_password_here

REDIS_LOCATION=redis://redis:6379/1
CELERY_BROKER_URL=redis://redis:6379/2
CELERY_RESULT_BACKEND=redis://redis:6379/3

EMAIL_HOST_USER=your_email_host_user_here
EMAIL_HOST_PASSWORD=your_email_host_password_here
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True

TELEGRAM_TOKEN=telegtam_token

DOCKER_HUB_USERNAME=your_docker_hub_username_here
DOCKER_HUB_TAG=docker_hub_ready_four_vpr_image_tag_here

BASE_SERVER_URL=localhost
````
4. Выполните миграции
````
python manage.py migrate
````
5. Запустите сервер
````
python manage.py runserver
````
6. Запустите Celery
````
celery -A config worker -l INFO
celery -A config beat -l INFO
````
## Архитектура проекта
### Модели
**Habit** (Привычка)
````
user — владелец
place — место выполнения
habit_time — время
action — действие
is_pleasant — признак приятной привычки
related_habit — связанная приятная привычка
periodicity — повтор (в днях, ≥ 1 и ≤ 7)
reward — вознаграждение
duration — длительность (не более 120 секунд)
is_public — публичная привычка
````
**CustomUser** (Пользователь)
````
email
username
Telegram chat_id
timezone
статус активации
по умолчанию авторизация по email
````
### Валидаторы

- нельзя одновременно указать reward и related_habit
- только приятные привычки могут быть связанной привычкой
- приятная привычка не может иметь reward или related_habit
- duration ≤ 120 секунд
- периодичность: 1–7 дней

### Права доступа

- Пользователь может CRUD только свои привычки
- Публичные привычки доступны только для чтения всем авторизованным пользователям
- Суперпользователь видит все привычки

### Интеграция с Telegram
**Проект отправляет сообщения через Telegram Bot API:**
- уведомление в указанное время согласно таймзоне пользователя
- периодичность учитывается автоматически
- приятные привычки добавляются в текст сообщения
````
Пример сообщения:

«Время выполнить привычку: выпить стакан воды. Это займёт всего 2 минуты — ты справишься! После выполнения ты получишь награду: конфету»
````

### Celery задачи
1. **send_habit_reminder + send_telegram_message**

Ежедневно проверяет:
- текущее время по таймзоне пользователя
- периодичность
- дату создания привычки

При совпадении условий отправляет напоминание в Telegram.

2. **send_activation_email**

Отправляет ссылку активации при регистрации нового пользователя.

Реализовано через отложенную Celery-задачу.

### Тестирование

**Покрытие тестами:** 90%

**Запуск тестов:**
````
coverage run --source='.' manage.py test
coverage report
````
### Документация API
Сгенерирована автоматически и доступна по адресу:
````
/swagger/
или
/redoc/
````

## Запуск проекта на удаленном сервере

Проект развёртывается на удалённом сервере с помощью Docker Compose и GitHub Actions.

**Адрес сервера с развернутым приложением:** 

### Архитектура

Django + DRF

PostgreSQL

Redis

Celery + Celery Beat

Gunicorn

Nginx

Docker / Docker Compose

GitHub Actions (CI/CD)

### Настройка удалённого сервера

**На сервере должны быть установлены:**
````
sudo apt update
sudo apt install -y docker.io docker-compose-plugin nginx
````

**Дополнительно:**

- пользователь добавлен в группу docker

- вход по SSH-ключу

- открыты порты 80, 443, 22

- проект размещается в директории:
/home/<user>/habitladder

### Переменные окружения

**Файл .env:**

- не коммитится в репозиторий

- используется на сервере и создаётся автоматически в GitHub Actions

### GitHub Secrets

В репозитории → Settings → Secrets and variables → Actions должны быть добавлены:

| Secret                    | Назначение              |
| ------------------------- |-------------------------|
| `DJANGO_SECRET_KEY`       | Django SECRET_KEY       |
| `DB_PASSWORD`             | Пароль PostgreSQL       |
| `EMAIL_HOST_USER`         | Почта                   |
| `EMAIL_HOST_PASSWORD`     | Пароль почты            |
| `TELEGRAM_TOKEN`          | Telegram token          |
| `BASE_SERVER_URL`         | Домен или IP сервера    |
| `DOCKER_HUB_USERNAME`     | Docker Hub username     |
| `DOCKER_HUB_ACCESS_TOKEN` | Docker Hub access token |
| `SSH_KEY`                 | Приватный SSH-ключ      |
| `SSH_USER`                | Пользователь сервера    |
| `SERVER_IP`               | IP сервера              |

### CI/CD (GitHub Actions)

Workflow расположен в .github/workflows/ci_cd.yaml

**Алгоритм workflow:**
- Lint
    - запуск линтеров

````
lint:
	isort . --check-only --diff
	black . --check
	flake8 .

format:
	isort .
	black .
````

- Tests

    - запуск Django-тестов

    - при ошибках деплой останавливается


- Build

    - сборка Docker-образа

    - push в Docker Hub


- Deploy

    - генерация .env

    - подмена server_name в nginx.conf

    - деплой на сервер через rsync

    - Cleanup .env из GitHub Actions runner

    - загрузка образов сервисов, указанных в docker-compose.yaml (docker compose pull)

    - запуск в фоновом режиме bd, redis

    - применение миграций
  
    - сборка статических файлов в контейнер с приложением

    - запуск сервисов в фоновом режиме

Pre-commit с линтерами запускается при каждом commit.
Workflow запускается автоматически при каждом push.

### Деплой приложения

Деплой происходит автоматически после успешного прохождения тестов.

Ручной деплой на сервере:
````
cd ~/habitladder
docker compose pull
docker compose up -d db redis
docker compose run --rm web python manage.py migrate
docker compose run --rm web python manage.py collectstatic --noinput
docker compose up -d
````

## Автор
Надежда Попова

Python Developer

📧 nadezhdapopova13@yandex.ru

🔗 GitHub: nadezhdapopova-spec
